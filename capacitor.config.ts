import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Monumento_Guayabo_Sendero',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
